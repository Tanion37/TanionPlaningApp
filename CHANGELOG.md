# Changelog

## 2026-08-19

- Этап 1: тег «соцсети» вместо проекта (миграция существующих); исполнитель «Юра» по умолчанию; кнопка бэклога переименована; на «Задачи дня» спрятаны надпись/область/переписка.
- «Надо закончить» раньше даты постановки подсвечивается красным, сохранение не блокируется.
- Кисть «Сегодня» прячет задачу с экрана дня на 4 часа (даты не меняются).
- Кнопка «Сделано и проверить»: выполнена + новая задача «Проверить …» со стартом завтра.
- Этап 2: задачи не-Юры — отдельные разделы на «Задачи дня», не в Горит/Нужно/Можно; кнопка «Отправить в Telegram» в чат PGD studio AI (@LexUor / @Dreeema / @dixi_alex).
- Этап 3: галочки и глаз в списках (экран и Telegram: именованные списки, задачи исполнителей, `/list`); синк списков в Notion.
- «Сделано и проверить» снимает тег «актуальная». В правке задачи те же пресеты, что при создании.
- Рабочий запуск – `TanionPlaning`; публичный `TanionPlaningApp` – снимок `app/` (`scripts/sync_public_app.ps1`).
- Этап 4: кисти Завтра / Неделя / Бэклог / Входящие по клику на раздел; входящие — кисть, не «всё сразу»; отмена массового действия за последние 5 секунд; экран «Бэклог» слева от «Списки».
- Этап 5: периодичность создаёт новую копию задачи (не двигает ту же); следующая появляется по календарю, только если в серии нет открытого экземпляра.
- Этап 6: токены читаются из `TanionAgentSetting/secrets.json` (ссылки `secrets:` в `config.json`).
- Кисть исполнителя: клик по разделу имени (Лёша / Саша / Сойер) берёт кисть; клик по задаче или другому разделу ставит этого исполнителя.
- Telegram: галочки и глаз в `/today`, теге, проекте, «горит»/«делаем» и `/list` (как в именованных списках).
- Кисть кнопок и тегов красит раздел по клику на заголовок; кружки тегов внизу и сбоку только берут кисть.
- Если бэкап в GitHub не удался – в ГОРИТ появляется «СДЕЛАТЬ БЭКАП»; после успешного бэкапа задача отмечается выполненной.
- Git: код приложения – публичный `TanionPlaningApp`; приватный `TanionPlaning` – только задачи, списки и личные файлы.

## 2026-08-16

- Тег бэклог: канон `бэклог`; алиасы `отложенная` / `беклог` / `backlog` с миграцией.
- Диалог новой задачи: пресеты «горящая» / «нужно» / «во входящие» / «в беклог» / «отметить выполненное».
- Поле «Исполнитель»; отдельный редактируемый список на экране «Списки» (по умолчанию Лёша, Саша, Сойер).
- Синхронизация с Notion: журнал изменений, debounce 1 мин, офлайн-ретрай ~час, суточная сверка; инструкция `docs/notion-sync.md`.

## 2026-08-13

- Краткая инструкция по планировщику: `docs/app-guide.md` (функции, установка, запуск, бот); README обновлён под текущие команды бота и ярлык.

## 2026-08-12

- Руководство пользователя portable: `docs/portable-user-guide.md` (экраны, теги, кисти, цвета, сценарии); ссылка в README; краткий `README.txt` в сборке.
- Черновик раздела «Настольные игры» для документа ОБРАЗ «Игровые форматы для образования»: `data/obraz_game_formats_education_boardgames.txt`.

## 2026-08-11

- Стиль письма v1.5: в постах probgs ссылки без префикса `https://`.
- Стиль письма v1.4: подборки ссылок; лексика тестирования («тестировщик»; «тест», не «плейтест»); правки автора в чате; в книге тоже «тестировщики».
- `cursor-master-guide.md`: отсылки к лексике тестирования, ссылкам без `https://` и явной команде на подстановку текста.

## 2026-08-06

- Бекап в 21:00 МСК: кроме GitHub, `tasks.xlsx` и `customers.json` уходят файлами в Telegram (sendDocument).
- Диалоги новой/правки задачи: компактные значки всех тегов (клик вкл/выкл), общий виджет `TagIconsPicker`.
- TanionTaskBot: бекап `data/tasks.xlsx` (+ `customers.json`) в приватный GitHub в 09:00, 12:00, 18:00, 21:00 МСК (`scripts/tasks_github_backup.py`); уведомление в Telegram.
- План миграции ботов/таблицы задач на Synology DS223: `docs/synology-ds223-migration-plan.md`.
- Undo/Redo (кнопки ↶/↷): многошаговый откат и повтор по журналу `data/logs` (`before_state`/`after_state`); повторный клик кистью по той же задаче — как Undo.
- TanionTaskBot: ежедневно в 23:50 — отчёт как «СЕГОДНЯ»; при старте догон за прошлые 7 дней с заголовком `ДД:ММ было сделано`; учёт в `bot_state.json` без затирания offset.

## 2026-08-04

- «ЧТО СДЕЛАНО СЕГОДНЯ» / алиас «СЕГОДНЯ»: в отчёт также задачи с меткой «контроль» за день; то же для «ВЧЕРА».
- TanionTaskBot: снят UTF-8 BOM у `start_bot.bat` (ломал автозапуск); лог `bot_run.log`; таймаут обработки сообщения и socket, чтобы один зависший апдейт не глушил бот.
- Неделя (кнопка/колонка НЕДЕЛЯ): снимает «актуальная» (как Завтра); «входящая» уже снималась при смене старта.
- Кнопка ↻: перезапуск приложения сразу в полном экране (для подтягивания обновлений).
- Заголовки Горит/Нужно/Можно — кисть (исправлен краш инициализации); клик по задачам = как drop в раздел.
- Вкладка xlsx `tags`: панель (ключ+символ), системные и скрытые теги; обновляется при сохранении.
- Новая задача на экране «Задачи дня»: старт = сегодня, тег «входящая».

## 2026-08-03

- Бот: «ЧТО СДЕЛАНО…» — название и теги (без номеров и дат); по имени списка (напр. «Лена») — содержание списка.
- «ЧТО СДЕЛАНО СЕГОДНЯ/ВЧЕРА»: учитывает все задачи, получившие «выполнена» за день (лог completed + кисть toggle); кисть выполнена/отменена пишет completed/cancelled и completed_at.
- Portable-сборка без Python/бота: `scripts/build_portable.ps1` → `C:\CURSOR\TanionPlaning-portable` (пустой `data\tasks.xlsx`).
- Завтра (кнопка/колонка ЗАВТРА): снимает «актуальная». Контроль: старт завтра + снимает «актуальная».
- ДЕНЬ: колонки балансируются по числу задач; при скрытии «Входящие» пересчитывается число колонок (отложенный rebuild + оценка ширины).
- Кисть: повторный клик по кнопке (Завтра/Неделя/тег) только снимает режим; отмена последнего изменения — повторным кликом по той же задаче.
- ДЕНЬ: разделы выровнены по верху колонки.
- Задачи дня: старт с экрана «Задачи дня»; в разделах ДЕНЬ сортировка как Горит/Нужно/Можно; разделы сначала по горизонтали, затем вниз, с равномерными вертикальными промежутками; заголовки ж.р. ед.ч. во мн.ч. (сложные, быстрые, …).
- Теги: контрольная→контроль, геймдизайнерская→геймдизайн, платёжная→финансы; кисть тега подсвечивает круги на «Задачи дня»; кнопка «Оч» убрана.
- Теги в ж.р. ед.ч.: важная, срочная, входящая, актуальная, проверенная, отложенная, …; «ответы» слиты в «переписка»; «в работе»/ПРОГД/ИИ без смены.
- Смена `start_at` (Завтра/Неделя/triage/ответы-переписка/беклог/правка) снимает «входящая».

## 2026-08-02

- Drag задачи из скролл-колонки: снятие с layout + `grabMouse` после reparent — можно сразу утащить за пределы колонки (Входящие / Горит–Делаем).
- Общее правило раскладки: плавающая панель кнопок не накладывается на задачи — контент сдвигается (`layout_metrics`); исправлен отступ на «Задачи дня».
- Задачи дня: раздел «без тега» в ДЕНЬ; отступ Входящих от правой панели; без заголовка «Задачи дня»; нижняя полоса тегов; DnD тегов в колонках ДЕНЬ; символ входящие ⬇ (кнопка «во входящие» тот же); фикс висящего блока после drop в Приоритет.
- Экран «Задачи дня» (между Списки и Горит/Делаем): Входящие, Горит/Нужно/Можно, зона ДЕНЬ; кнопка «Всё во входящие»; панель слева при непустых входящих.
- Системные теги: входящие, актуально, важно, срочно, выполнена, отменена, проверено; fuzzy по 1–2 последним буквам; теги дней недели удалены (экран «По дням недели» убран).
- Рамки по тегам (оранж/синий/зелёный/серый); режим «кисть» тега/кнопки с undo последнего действия.

## 2026-07-29

- Журнал действий с задачами (`data/logs/YYYY-MM-DD.jsonl`): перенос, изменение, выполнение, отмена; экран «Логи» слева от «Списки» (колонки по дням: выполненные → изменённые → все по времени).
- Бот: команды `ЧТО СДЕЛАНО СЕГОДНЯ` / `ЧТО СДЕЛАНО ВЧЕРА` (без учёта регистра).

- fix: читать лист `tasks` явно (не wb.active) — после Excel иначе подхватывался `lists` и задачи «пропадали»; save сохраняет другие вкладки.

- Экран «Списки»: клик по всей области под заголовком колонки добавляет пункт.
- Тег ПРОГД (синие буквы «ПРО» на кружке).
- НЕДЕЛЯ: задачи со start > today+7 не попадают в колонку; ГОРИТ при drop не ставит тег дня.
- Экран «Списки» слева от triage; вкладка `lists` в xlsx; клик по пустому месту колонки — пункт; бот: `ДОБАВЬ … В …` / `ДОБАВЬ В …`.
- Календарь: стрелки года до 2100 без красного отката; пустая дата открывается на текущем месяце.
- Напоминание: поле времени ЧЧ:ММ; если задано — TanionTaskBot шлёт уведомление в Telegram в эту дату/время.
- Тег «контроль» (👁); «сложная» с иконкой 🐸 (лягушка слита); беклог / ТУМАН ВОЙНЫ → старт +1 месяц, без due/remind, без важная/срочная.
- Кнопка «Оч» (очистка) под «очертить область»: снимает теги важная и срочная со всех задач.

## 2026-07-28

- Проекты без учёта регистра: дубликаты вроде «Игра про храмы» / «Игра про Храмы» объединяются; сравнение и колонки case-insensitive.
- New/Edit задача: стартовая ширина ≤ окно приложения, ресайз Windows свободен; список проектов — горизонтальный скролл, сортировка по частоте использования.
- TanionTaskBot: xlsx; утро 08:00 ГОРИТ/ДЕЛАЕМ; чтение по тегу/проекту; добавление `новая`/`тег`/`проект`/`старт`/`срок`; только add+read (без изменения задач).
- Fullscreen: скрывать кнопки ◀▶.
- EditTaskDialog: ширина ≤ окно приложения; теги со скроллом по горизонтали; порядок тегов по частоте использования.
- Triage: ГОРИТ исключает `start_at > today`; ДЕЛАЕМ — `start_at <= today`; зона ГОРИТ слева со свободным DnD.
- Drop в колонку подстраивает даты/теги (ГОРИТ→due сегодня; НЕДЕЛЯ→`start_at=today+7` без тега дня).
- Кнопки Сегодня/Завтра/Неделя = те же мутации, что drop в ДЕЛАЕМ/ЗАВТРА/НЕДЕЛЯ; ДЕМО в правом нижнем углу.
- DEMO: только demo-задачи на всех экранах/фильтрах; сброс фильтра при входе.
- Если ▶ пересекается с правой кнопкой — эта кнопка и все ниже смещаются вниз.

## 2026-07-27

- Экран по умолчанию «ГОРИТ / ДЕЛАЕМ / ЗАВТРА / НЕДЕЛЯ / ТУМАН ВОЙНЫ» (колонки справа налево, скролл в колонке).
- Кнопки Сегодня / Завтра / Неделя / Беклог / Ответы; weekday-теги при Сегодня/Завтра/Неделя; тег «ответы» (старт +7 дней).
- Новая задача: пустые постановка и окончание; Delete очищает даты; due &lt; start — откат с подсветкой; кнопки проектов в New как в Edit.
- ДЕМО-режим (левый нижний угол); надписи и области (`data/board_annotations.json`); Aa / ▢ справа.
- Периодичность «каждый день/неделю»: при старте сдвиг просроченных start/due на текущий период.
- Приоритет стрелок ◀▶ над правой панелью при пересечении; ярлык с AppUserModelID (`create_desktop_shortcut.ps1`).

## 2026-07-18

- Move done/cancel tags after weekdays in default order; remove green + button; taskbar icon via AppUserModelID and Desktop shortcut (assets/app_icon.ico, create_desktop_shortcut.ps1).

- Remove duplicate MIME_TAG constant from widgets.py.
- Drag тега на задачу (тач/мышь): тег копируется на задачу, сам остаётся на месте.
- Drag тега на другой тег: меняются местами на всех экранах (`data/tag_order.json`).
- Иконки: «подумать» → 💡, «беклог» → 🗄; иконка приложения – календарь (`assets/app_icon.png`).
- Тег **выполненное** (✅): дата выполнения, серые рамка/текст; видно только в фильтре тега и в списке проекта.
- Тег **отменённое** (🗑): серые рамка/текст + зачёркивание; видно только в фильтре тега и в списке проекта.
- Оба тега в нижней панели; с обычных экранов скрыты.
- Клик по пустому месту холста → диалог новой задачи (с координатой).
- Десктоп-планировщик (`app/`): задачи в xlsx, блоки 200×50, DnD на теги, свайп-экраны, fullscreen.
- Поля задачи: проект, расширенное описание; двойной клик — редактирование и теги.
- Экран «По проектам»; клик по названию проекта — фильтр задач проекта.
- Пути и правило projects-home: репозиторий в `D:\Cursor\TanionPlaning`.
- docs/VERSIONING.md: ссылка на обязательный CHANGELOG (§3.4 tas).
- Синхронизация `.cursor/rules/tanion-global.mdc` с правилом CHANGELOG.


## 2026-08-06
- Add scripts/ensure_bot.ps1 for logon/watchdog autostart.

